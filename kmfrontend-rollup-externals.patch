--- a/kmfrontend/vite.config.ts
+++ b/kmfrontend/vite.config.ts
@@ -6,6 +6,11 @@ import { nodePolyfills } from 'vite-plugin-node-polyfills';
 export default defineConfig({
 	build: {
 		sourcemap: true,
+		rollupOptions: {
+			external: [
+				/^[.\/]+\/src\/lib\/.*/
+			]
+		},
 	},
 	plugins: [nodePolyfills(), react(), legacy()],
 	server: {
